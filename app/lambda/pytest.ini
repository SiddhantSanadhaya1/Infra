[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: mark test as a unit test
addopts =
    -v
    --strict-markers
    --tb=short
    --disable-warnings
